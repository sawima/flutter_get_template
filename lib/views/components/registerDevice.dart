import 'package:flutter/material.dart';
class RegisterDevicePage extends StatelessWidget {
  const RegisterDevicePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("register device page"),
    );
  }
}
